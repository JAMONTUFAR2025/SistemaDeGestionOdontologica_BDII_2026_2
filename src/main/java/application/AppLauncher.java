package application;

public class AppLauncher {
    // Touch to force rebuild - Removed quick days actions and fixed default date format
    public static void main(String[] args) {
        Main.main(args);
    }
}
