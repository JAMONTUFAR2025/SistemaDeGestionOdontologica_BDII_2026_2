package application;

public class AppLauncher {
    // Touch to force rebuild - Make phone and identity optional, fallback to responsible phone
    public static void main(String[] args) {
        Main.main(args);
    }
}
